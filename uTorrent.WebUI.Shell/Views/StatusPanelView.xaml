<UserControl x:Class="uTorrent.WebUI.Shell.Views.StatusPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid Grid.Row="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Text="{Binding StatusMessage, Mode=OneWay}" FontSize="8" HorizontalAlignment="Left" VerticalAlignment="Center" TextWrapping="Wrap" />
        <Button Grid.Column="1" Content="Settings" Command="{Binding SettingsCommand}" />
        <Button Grid.Column="2" Content="Clear" Command="{Binding ClearCommand}" />        
    </Grid>
</UserControl>
